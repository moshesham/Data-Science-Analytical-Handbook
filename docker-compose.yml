version: '3.8'

services:
  jekyll:
    image: jekyll/jekyll:4
    working_dir: /srv/jekyll
    volumes:
      - ./:/srv/jekyll:Z
    ports:
      - "4000:4000"
    command: jekyll serve --watch --force_polling --host 0.0.0.0 --livereload --port 4000
    environment:
      - JEKYLL_ENV=development
